# Sitio anterior (legacy)

Snapshot del portfolio que estuvo online en `https://fb91.github.io` hasta el rediseño de 2026.

Stack original: HTML/CSS plano + Bootstrap 4 + jQuery + parallax.js.

Las imágenes (`img/thumb` y `img/large`) están duplicadas en `/public/archivo/` para que el sitio nuevo las exponga en `/archivo`.

Este directorio se conserva por referencia. Podés borrarlo cuando quieras.
